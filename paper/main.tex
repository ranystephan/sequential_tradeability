\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath, amssymb, amsthm, bm}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{hyperref}

\title{Sequential Tradeability Testing for Alpha Signals}
\author{Rany Stephan}
\date{\today}

\newtheorem{proposition}{Proposition}

\begin{document}
\maketitle

\begin{abstract}
Many candidate alpha signals have positive historical predictive content but fail after
uncertainty, decay, turnover, and market impact are considered.  This project studies
alpha validation as a sequential decision problem.  Starting from the Bayesian drift
estimation framework of Ekstrom, Karatzas, and Vaicenavicius, we model the evidence for
an alpha signal as a noisy observation process with an unknown drift.  The posterior mean
and posterior variance form the state variables, and the decision to continue observing,
activate, or retire the signal becomes an optimal stopping problem.  The first step of the
project derives and verifies the filtering identities that will later be extended with
transaction costs and alpha decay.
\end{abstract}

\section{Base Sequential Estimation Model}

Let \(X\) denote the unknown strength of a candidate alpha signal.  In the base model,
we observe a noisy evidence process
\begin{equation}
    Y_t = X t + W_t, \qquad t \ge 0,
    \label{eq:observation_model}
\end{equation}
where \(W\) is a standard Brownian motion independent of \(X\).  The prior distribution
of \(X\) is denoted by \(\mu\), and the observation filtration is
\[
    \mathcal F_t^Y = \sigma(Y_s:0\le s\le t).
\]
In the alpha interpretation, \(Y_t\) can be read as cumulative signal evidence, such as
cumulative signal-aligned residual return or cumulative information coefficient, after
appropriate normalization.

The least-squares estimate of \(X\) is the posterior mean
\begin{equation}
    \widehat X_t = \mathbb E[X\mid \mathcal F_t^Y].
\end{equation}
For any prior with finite second moment, Bayes' rule gives the posterior distribution
\begin{equation}
    \mu_{t,y}(du)
    =
    \frac{\exp\{uy-u^2t/2\}\mu(du)}
    {\int_{\mathbb R}\exp\{vy-v^2t/2\}\mu(dv)}.
    \label{eq:posterior}
\end{equation}
Thus the posterior mean and posterior variance are
\begin{align}
    G(t,y)
    &=
    \frac{\int_{\mathbb R}u\exp\{uy-u^2t/2\}\mu(du)}
    {\int_{\mathbb R}\exp\{uy-u^2t/2\}\mu(du)},\\
    H(t,y)
    &=
    \int_{\mathbb R}(u-G(t,y))^2\mu_{t,y}(du).
\end{align}
The estimate is therefore \(\widehat X_t=G(t,Y_t)\).

\section{Filtering Identity}

The innovation process is
\begin{equation}
    \widehat W_t
    =
    Y_t-\int_0^t \widehat X_s\,ds.
\end{equation}
It is a Brownian motion in the observation filtration.  If \(G_t(\cdot)=G(t,\cdot)\)
and
\begin{equation}
    \Psi(t,x) = H(t,G_t^{-1}(x)),
    \label{eq:psi_definition}
\end{equation}
then the posterior mean satisfies
\begin{equation}
    d\widehat X_t = \Psi(t,\widehat X_t)\,d\widehat W_t.
    \label{eq:posterior_mean_sde}
\end{equation}
The same function \(\Psi\) is also the conditional mean-squared error:
\begin{equation}
    \Psi(t,\widehat X_t)
    =
    \operatorname{Var}(X\mid \mathcal F_t^Y).
\end{equation}
This dual role is the mathematical reason the problem admits a clean stopping
formulation: posterior uncertainty is both the remaining estimation error and the local
volatility of the posterior mean.

\section{Optimal Stopping Interpretation}

Ekstrom, Karatzas, and Vaicenavicius study
\begin{equation}
    \inf_{\tau}
    \mathbb E\left[(X-\widehat X_\tau)^2+c\tau\right],
    \label{eq:ekv_objective}
\end{equation}
where \(c>0\) is the observation cost per unit time.  Since
\[
    \mathbb E[(X-\widehat X_\tau)^2]
    =
    \mathbb E[\Psi(\tau,\widehat X_\tau)],
\]
the objective can be written in posterior-variance form.

\begin{proposition}[EKV variance decay identity]
For any admissible stopping time \(\tau\),
\begin{equation}
    \mathbb E[\Psi(\tau,\widehat X_\tau)]
    =
    \operatorname{Var}(X)
    -
    \mathbb E\left[\int_0^\tau \Psi^2(s,\widehat X_s)\,ds\right].
    \label{eq:variance_decay_identity}
\end{equation}
\end{proposition}

Consequently, minimizing \eqref{eq:ekv_objective} is equivalent, up to the constant
\(\operatorname{Var}(X)\), to minimizing
\begin{equation}
    \inf_{\tau}
    \mathbb E\left[
    \int_0^\tau
    \left(c-\Psi^2(s,\widehat X_s)\right)ds
    \right].
    \label{eq:running_cost_form}
\end{equation}
The interpretation is direct: observing for one more instant costs \(c\), while the
instantaneous rate of posterior-variance reduction is \(\Psi^2\).  The continuation
region is therefore the set where the option value of further learning exceeds the
observation cost.

\section{Two Checkable Priors}

\subsection{Gaussian prior}

If \(X\sim N(m,\sigma^2)\), then
\begin{equation}
    \widehat X_t
    =
    \frac{m+\sigma^2Y_t}{1+\sigma^2t},
    \qquad
    \Psi(t,x)
    =
    \frac{\sigma^2}{1+\sigma^2t}.
\end{equation}
The posterior variance is deterministic.  The optimal stopping time is
\begin{equation}
    \tau^\star
    =
    \left(\frac{1}{\sqrt c}-\frac{1}{\sigma^2}\right)^+.
    \label{eq:gaussian_tau}
\end{equation}
This case will serve as our first validation benchmark: the code must reproduce the
precision update and the variance decay identity exactly.

\subsection{Symmetric Bernoulli prior}

If \(X\in\{-\beta,\beta\}\), then the posterior mean lies in \((-\beta,\beta)\) and
\begin{equation}
    \Psi(x)=\beta^2-x^2.
\end{equation}
When \(\beta^4\le c\), immediate stopping is optimal.  Otherwise the optimal stopping
time is the first exit from a symmetric interval:
\begin{equation}
    \tau_a^\star
    =
    \inf\{t\ge 0:|\widehat X_t|\ge a\},
\end{equation}
where \(a\in(\gamma,\beta)\), \(\gamma=\sqrt{\beta^2-\sqrt c}\), solves
\begin{equation}
    \int_0^a
    \frac{c-(\beta^2-\xi^2)^2}{(\beta^2-\xi^2)^2}\,d\xi
    =
    0.
    \label{eq:bernoulli_boundary}
\end{equation}
This case is the first mathematical template for alpha activation or rejection: continue
while the evidence is ambiguous, and stop once posterior confidence is sufficiently far
from zero.

\section{Project Translation}

The base paper estimates an unknown drift.  Our project will preserve the posterior
state and stopping-boundary structure, but change the terminal payoff from estimation
accuracy to economic tradeability.  The intended mapping is:
\[
\begin{array}{lll}
\text{EKV variable} & & \text{alpha tradeability variable}\\
\midrule
X && \text{unknown alpha strength}\\
Y_t && \text{cumulative normalized signal evidence}\\
\widehat X_t && \text{posterior mean alpha}\\
\Psi(t,\widehat X_t) && \text{posterior uncertainty and learning speed}\\
c && \text{observation, opportunity, and implementation cost}\\
\tau && \text{activation, rejection, or retirement time}.
\end{array}
\]
The next mathematical step is to define a finance terminal payoff
\(\Phi(m,q;\mathrm{cost})\) that rewards activation only when posterior alpha value
survives uncertainty and transaction costs.  Once that payoff is fixed, the project
becomes a numerically solvable optimal stopping problem whose boundary can be compared
against fixed \(t\)-statistic, rolling Sharpe, and IC-threshold rules.

\section{A First Tradeability Payoff}

We now define the first finance version of the terminal stopping payoff.  Suppose that,
at a stopping time, the posterior belief about the signal strength is summarized by mean
\(m\) and variance \(q\).  If the signal is activated with signed exposure \(z\), we use the
posterior predictive mean-variance objective
\begin{equation}
    m z
    -
    \frac{\lambda}{2}(\sigma_r^2+q)z^2
    -
    \kappa.
    \label{eq:activation_objective}
\end{equation}
Here \(\lambda>0\) is risk aversion, \(\sigma_r^2\) is the residual return variance of the
traded signal, \(q\) is posterior parameter uncertainty, and \(\kappa\ge 0\) is an
implementation hurdle representing spread, impact, research delay, or capital allocation
cost.  Optimizing \eqref{eq:activation_objective} over \(z\in\mathbb R\) gives
\begin{equation}
    \sup_z
    \left\{
    mz-\frac{\lambda}{2}(\sigma_r^2+q)z^2-\kappa
    \right\}
    =
    \frac{m^2}{2\lambda(\sigma_r^2+q)}-\kappa.
\end{equation}
Since the decision maker can reject the signal instead of trading it, the terminal payoff is
\begin{equation}
    \Phi(m,q)
    =
    \left(
    \frac{m^2}{2\lambda(\sigma_r^2+q)}
    -
    \kappa
    \right)^+.
    \label{eq:tradeability_payoff}
\end{equation}
The immediate activation threshold is therefore
\begin{equation}
    |m|
    >
    \sqrt{2\lambda(\sigma_r^2+q)\kappa}.
    \label{eq:activation_threshold}
\end{equation}
This is only a terminal threshold.  Before the final validation date, the signal also has
option value: a currently ambiguous signal may become tradeable after more evidence
arrives.

\section{Finite-Horizon Stopping Problem}

For the Gaussian prior, the posterior variance is deterministic:
\[
    q(t)=\frac{q_0}{1+q_0t},
\]
and the posterior mean follows
\begin{equation}
    dm_t = q(t)\,d\widehat W_t.
\end{equation}
Given a finite validation horizon \(T\), the tradeability value is
\begin{equation}
    V(t,m)
    =
    \sup_{\tau\in[t,T]}
    \mathbb E_{t,m}
    \left[
        e^{-\rho \tau}\Phi(m_\tau,q(\tau))
        -
        \int_t^\tau c\,ds
    \right].
    \label{eq:tradeability_value}
\end{equation}
The parameter \(\rho\ge 0\) captures alpha decay or opportunity loss during validation.
This term is economically important.  Without decay or discounting, the activation
payoff is convex in the posterior mean \(m\), and the model can prefer to keep observing
even when \(|m|\) is already very large.  That is the wrong lifecycle decision for a
decaying alpha: a sufficiently strong signal should be activated before its deployable
value evaporates.

The continuation region satisfies the variational inequality
\begin{equation}
    \max\left\{
        e^{-\rho t}\Phi(m,q(t))-V(t,m),
        \partial_t V(t,m)
        +
        \frac{1}{2}q(t)^2\partial_{mm}V(t,m)
        -
        c
    \right\}
    =
    0,
    \label{eq:tradeability_vi}
\end{equation}
with terminal condition
\begin{equation}
    V(T,m)=e^{-\rho T}\Phi(m,q(T)).
\end{equation}

This is the first paper-level object we can compute.  It has the right economic
interpretation:
\begin{itemize}
    \item high \(|m|\) favors stopping and activating the signal;
    \item low posterior variance \(q\) lowers the option value of more observation;
    \item high observation or implementation cost shrinks the continuation region;
    \item near \(m=0\), the payoff can be zero while continuation value remains positive,
    because future evidence may still move the posterior mean into the tradeable region.
\end{itemize}

The code solves \eqref{eq:tradeability_vi} by backward implicit Euler on a uniform grid in
\((t,m)\), with the mean-domain boundaries pinned to the immediate stopping payoff.  The
implementation is validated against exact activation thresholds, symmetry in \(m\), the
zero-payoff no-continuation case, monotonic decline of value as observation cost rises, and
early exercise of strong alpha when \(\rho>0\).

\begin{figure}[t]
    \centering
    \includegraphics[width=0.92\linewidth]{../figures/gaussian_tradeability_boundary.pdf}
    \caption{First finite-difference tradeability boundary for the Gaussian-belief model.
    Red states stop and activate the alpha, gray states stop and reject it, and blue states
    continue observing.  The dashed curves show the static activation threshold from
    \eqref{eq:activation_threshold}; the dynamic boundary is wider because ambiguous
    signals have option value early in the validation period.}
    \label{fig:gaussian_tradeability_boundary}
\end{figure}

\section{Simulation Validation}

Before using real data, we validate the stopping rule inside the model it is designed to
solve.  We simulate evidence paths
\begin{equation}
    dY_t = \theta\,dt+dW_t
\end{equation}
for fixed true alpha strengths \(\theta\).  The posterior mean and variance are updated
exactly using the Gaussian formulas above:
\begin{equation}
    m_t = \frac{q_0Y_t}{1+q_0t},
    \qquad
    q_t = \frac{q_0}{1+q_0t}.
\end{equation}
If a policy activates at time \(\tau\), it chooses the posterior-optimal exposure
\begin{equation}
    z_\tau^\star
    =
    \frac{m_\tau}{\lambda(\sigma_r^2+q_\tau)}.
\end{equation}
We then evaluate the realized economic value under the true alpha:
\begin{equation}
    e^{-\rho\tau}
    \left(
        \theta z_\tau^\star
        -
        \frac{\lambda}{2}\sigma_r^2(z_\tau^\star)^2
        -
        \kappa
    \right)
    -
    c\tau,
    \label{eq:realized_value}
\end{equation}
with value \(-c\tau\) if the policy rejects instead of activating.

Figure~\ref{fig:simulation_policy_comparison} compares the dynamic boundary to two
simple baselines: immediate activation once the static payoff threshold
\eqref{eq:activation_threshold} is crossed, and a fixed posterior \(z\)-statistic rule.
The experiment is not meant to be the final empirical result.  It is a calibration check.
It shows that the dynamic rule captures option value for genuinely strong alphas, but it
also exposes a weakness of the Gaussian prior: when the true alpha is zero, random
evidence often eventually looks tradeable.  This motivates the next model extension, a
dead/alive or spike-and-slab prior that can explicitly represent the possibility that a
candidate alpha is worthless.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.86\linewidth]{../figures/simulation_policy_comparison.pdf}
    \caption{Pathwise validation under the Gaussian evidence model.  The dynamic boundary
    substantially improves realized value for large \(|\theta|\), but it is not uniformly
    dominant: near \(\theta=0\), the Gaussian prior over-activates noisy dead signals.  This
    is a useful stress test, pointing to an explicit dead/alive prior as the next
    mathematical extension.}
    \label{fig:simulation_policy_comparison}
\end{figure}

\section{Dead/Alive Prior}

The Gaussian prior is a natural first model because it gives a one-dimensional posterior
mean and a deterministic posterior variance.  But it does not assign positive probability
to the event that the signal has exactly zero alpha.  For signal validation, this is a
serious omission: many candidate alphas are not merely weak, but dead.

The next model is therefore the finite prior
\begin{equation}
    \theta\in\{-a,0,+a\},
    \qquad
    \mathbb P(\theta=-a)=p_-,
    \quad
    \mathbb P(\theta=0)=p_0,
    \quad
    \mathbb P(\theta=+a)=p_+.
    \label{eq:three_state_prior}
\end{equation}
The three states have direct economic meanings: live short, dead, and live long.  Given
evidence \(Y_t=y\), Bayes' rule gives
\begin{equation}
    \pi_i(t,y)
    =
    \frac{
        p_i\exp\{\theta_i y-\theta_i^2t/2\}
    }{
        \sum_j p_j\exp\{\theta_j y-\theta_j^2t/2\}
    },
    \qquad
    \theta_i\in\{-a,0,+a\}.
    \label{eq:three_state_posterior}
\end{equation}
The posterior mean and variance are
\begin{equation}
    m(t,y)=\sum_i\pi_i(t,y)\theta_i,
    \qquad
    q(t,y)=\sum_i\pi_i(t,y)\theta_i^2-m(t,y)^2.
    \label{eq:three_state_moments}
\end{equation}
As in the EKV filtering setup, the posterior mean is the innovation drift:
\begin{equation}
    dY_t=m(t,Y_t)\,dt+d\widehat W_t.
    \label{eq:three_state_innovation}
\end{equation}
The tradeability payoff can now be evaluated using \(m(t,y)\) and \(q(t,y)\), but the
state has an additional interpretation through the posterior dead probability
\(\pi_0(t,y)\).  If evidence remains near zero while time passes, the likelihood of the
dead state rises relative to the live states, because live alphas would probably have
generated directional evidence.

The first stress test showed that merely adding a dead state is not enough.  If the
terminal payoff remains only the trading payoff in \eqref{eq:tradeability_payoff}, the
model still activates too many noisy null paths.  This is not a numerical failure; it is
an objective-function issue.  A research process cares not only about expected trading
profit conditional on current beliefs, but also about the cost of admitting false
discoveries into production.  We model this with a dead-activation penalty \(\eta\ge0\),
paid if the signal is activated and \(\theta=0\).  Ex ante, the posterior expected penalty
is \(\eta\pi_0(t,y)\), so the terminal payoff becomes
\begin{equation}
    \Phi_\eta(t,y)
    =
    e^{-\rho t}
    \left(
    \frac{m(t,y)^2}{2\lambda(\sigma_r^2+q(t,y))}
    -
    \kappa
    -
    \eta\pi_0(t,y)
    \right)^+.
    \label{eq:dead_alive_payoff}
\end{equation}

This model is not just a larger portfolio problem.  A multiperiod portfolio model usually
takes the alpha signal as an input and chooses positions.  Here the decision comes one
step earlier: should this noisy signal be admitted into the trading system, rejected, or
observed longer?  That distinction is what makes the dead state valuable.

The implemented posterior object is validated by the following identities:
\begin{itemize}
    \item posterior probabilities sum to one and remain positive;
    \item under a symmetric prior, \(\pi_+(t,y)=\pi_-(t,-y)\) and
    \(m(t,y)=-m(t,-y)\);
    \item when \(y=0\), \(\pi_0(t,0)\) increases with \(t\);
    \item large positive or negative evidence identifies the corresponding live state;
    \item the EKV identity \(\partial_y m(t,y)=q(t,y)\) holds numerically;
    \item conditional innovation increments are centered under the posterior.
\end{itemize}
The next computational step is to solve the finite-horizon obstacle problem in
\((t,y)\)-space,
\begin{equation}
    \max\left\{
        \Phi_\eta(t,y)-V(t,y),
        \partial_tV+m(t,y)\partial_yV+\frac{1}{2}\partial_{yy}V-c
    \right\}
    =
    0.
    \label{eq:three_state_vi}
\end{equation}
The main testable prediction is that the dead/alive policy should reduce false activation
when \(\theta=0\), while preserving activation power when \(\theta=\pm a\).

\section{Dead/Alive Simulation Stress Test}

The calibrated dead/alive experiment uses \(p_0=0.70\), symmetric live probabilities,
matched prior second moment \(0.045\), and therefore
\[
    a=\sqrt{\frac{0.045}{1-p_0}}\approx0.387.
\]
The false-discovery cost is set to \(\eta=0.002\), with the same trading payoff,
observation cost, activation decay, and validation horizon used in the Gaussian
comparison.  The Gaussian benchmark is given the same initial prior variance, so the
difference is not simply more or less initial uncertainty; it is the explicit posterior
dead state and its admission penalty.

The finite-difference diagnostics are exact to numerical tolerance: the maximum obstacle
violation is \(0\), the terminal value error is \(0\), and the symmetry error is
\(4.2\times10^{-16}\).  The posterior dead probability at \(y=0\) increases from \(0.70\)
to \(0.731\) over the horizon, as expected when no directional evidence arrives.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.94\linewidth]{../figures/dead_alive_policy_comparison.pdf}
    \caption{Controlled stress test for the dead/alive admission rule.  The dead/alive
    boundary lowers false activation at \(\theta=0\) from \(47.1\%\) under the matched
    Gaussian boundary to \(39.8\%\), improving null realized value from \(-0.00560\) to
    \(-0.00513\).  The gain is not free: live-alpha value is slightly lower than the
    Gaussian benchmark.  The result is therefore a disciplined false-discovery/power
    tradeoff, not a dominance claim.}
    \label{fig:dead_alive_policy_comparison}
\end{figure}

\section{First Real-Data Experiment}

As a first real-data test, we use the Chen--Zimmermann Open Source Cross-Sectional Asset
Pricing data, specifically the Federal Reserve file of monthly long-short returns for 205
predictors.\footnote{\url{https://www.federalreserve.gov/econres/feds/open-source-cross-sectional-asset-pricing.htm}}
This is not yet the final WRDS stock-level experiment, but it is a useful intermediate
test because the inputs are real anomaly portfolio returns rather than simulated evidence.

For each signal, monthly returns are converted into Brownian evidence increments.  Let
\(\widehat\sigma_m\) be the monthly volatility estimated from a 120-month calibration
window.  With \(dt=1/12\),
\[
    \Delta Y_t
    =
    \frac{r_t}{\widehat\sigma_m}\sqrt{dt}.
\]
Thus a dead signal should have increment variance approximately \(dt\), while the drift
\(\theta\) is interpretable as an annualized Sharpe-like evidence strength.

The empirical protocol uses a common modern start date, January 1970, followed by 120
months of calibration, up to 240 months of sequential validation, and a 120-month
post-decision holdout window.  The admission rule uses the same three-state prior as the
simulation, but with a stricter false-discovery penalty \(\eta=0.04\).  The penalty is
calibrated by a placebo stress test: sign-flipping each signal's validation returns keeps
the realized volatility pattern but destroys stable directional drift.

Figure~\ref{fig:osap_penalty_sweep} shows the penalty sensitivity.  With no additional
false-discovery cost, the rule activates \(91.1\%\) of real OSAP signals but also
\(60.4\%\) of placebo signals.  As \(\eta\) increases, both rates decline, but the placebo
rate falls faster.  The choice \(\eta=0.04\) is a conservative middle point: real
activation remains high, while placebo activation falls below one third.  A larger
penalty such as \(\eta=0.08\) lowers placebo activation further, but at the cost of
rejecting many more real anomaly portfolios.

For eight canonical signals (short-term reversal, momentum, book-to-market, operating
profitability, asset growth, gross profitability, accruals, and bid-ask spread), the rule
activates six and rejects two.  The activated group has an average 120-month holdout
return of \(1.30\%\) per month.  Across all 192 OSAP predictors with enough post-1970
history, the rule activates 140 signals, or \(72.9\%\).  On the sign-flipped placebo
versions of the same signals, it activates only 52 signals, or \(27.1\%\).  This is the
first real-data evidence that the admission rule is not merely responding to volatility:
it is much more likely to stop and activate when the evidence has persistent direction.

The main benchmark question is whether this is genuinely a sequential result or just a
fixed-horizon posterior test in disguise.  To test this, we compare against fixed-horizon
rules at 12, 24, 60, 120, and 240 validation months.  Each rule uses the same
three-state posterior and is calibrated on the sign-flipped placebo signals to activate
exactly 52 placebos, matching the sequential rule's \(27.1\%\) placebo activation rate.
At a fixed horizon, posterior net payoff, posterior \(z\)-statistic, and posterior alive
probability give the same ranking in this symmetric three-state model; this is a useful
sanity check because all three are monotone transformations of absolute evidence
strength at fixed \(t\).

Figure~\ref{fig:osap_static_benchmark} shows the result.  The sequential rule activates
\(72.9\%\) of real signals with an average decision time of 47.9 months and an activated
holdout return of \(0.747\%\) per month.  A fixed 60-month test is close in time but
slightly weaker, activating \(70.8\%\) with \(0.705\%\) holdout return.  Fixed 120- and
240-month tests activate more real signals, \(77.1\%\) and \(82.8\%\), but they require
waiting 10 and 20 years and their activated holdout returns fall to \(0.689\%\) and
\(0.537\%\).  Thus the sequential rule does not simply dominate every fixed test on raw
activation count.  Its empirical advantage is an earlier admission frontier: it reaches
nearly the long-horizon activation power while preserving stronger post-decision
performance.  The activation-weighted holdout return is \(0.545\) for the sequential
rule, compared with \(0.500\), \(0.531\), and \(0.444\) for the 60-, 120-, and 240-month
fixed tests.

We also check numerical stability of the variational inequality solver.  Table
\ref{tab:grid_convergence} compares coarser grids with a fine \(481\times 1001\)
reference.  The paper grid has initial value error below \(9\times 10^{-5}\) and less
than \(1\%\) region disagreement on a common time-evidence evaluation grid.  Boundary
locations move by a few hundredths of a Brownian evidence unit, so the results should be
interpreted as economically stable rather than exact to the last grid point.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.82\linewidth]{../figures/osap_penalty_sweep.pdf}
    \caption{Penalty sensitivity on all 192 eligible OSAP predictors.  Increasing the
    false-discovery penalty lowers activation for both real signals and sign-flipped
    placebos, but it lowers placebo activation more quickly.  The dashed line marks the
    \(\eta=0.04\) calibration used in the real-data experiment.}
    \label{fig:osap_penalty_sweep}
\end{figure}

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{../figures/osap_static_benchmark_comparison.pdf}
    \caption{Fixed-horizon benchmarks calibrated to the same sign-flip placebo activation
    count as the sequential rule.  The left panel compares real activation rates.  The
    right panel compares activation-weighted holdout return, defined as real activation
    rate times average post-decision return among activated signals.}
    \label{fig:osap_static_benchmark}
\end{figure}

\begin{table}[t]
\centering
\caption{Grid convergence for the three-state OSAP stopping problem.}
\label{tab:grid_convergence}
\begin{tabular}{lrrrr}
\toprule
Grid & \(n_t\times n_y\) & \(V(0,0)\) & \(|\Delta V|\) & Region disagreement \\
\midrule
Coarse & \(121\times 401\) & 0.007155 & \(2.74\times 10^{-4}\) & 2.02\% \\
Medium time & \(241\times 401\) & 0.006988 & \(1.08\times 10^{-4}\) & 0.89\% \\
Medium space & \(241\times 601\) & 0.006976 & \(9.50\times 10^{-5}\) & 0.86\% \\
Paper & \(241\times 801\) & 0.006969 & \(8.87\times 10^{-5}\) & 0.90\% \\
Fine & \(481\times 1001\) & 0.006881 & 0 & 0 \\
\bottomrule
\end{tabular}
\end{table}

The approximation is imperfect.  Across all eligible signals, the mean validation
increment variance divided by \(dt\) is \(1.24\), and the median absolute lag-one
autocorrelation is \(0.10\).  These diagnostics are close enough to make the Brownian
evidence model a reasonable first approximation, but they also identify what the WRDS
extension should improve: stock-level construction, explicit transaction costs, and
signal-specific volatility updating.  In the stock-level extension, the same admission
rule should be run on gross and net long-short returns built from CRSP/Compustat data.
The required panel is one row per signal, month, and stock, with the signal value known
at formation, next-month return including delisting returns, market equity, a liquidity
or spread proxy, and enough identifiers to enforce common-stock and exchange filters.
This would turn the current question from ``does the anomaly return stream look
tradeable?'' into the more implementable question: ``does it remain tradeable after
portfolio turnover and trading frictions?''

\end{document}
